.DS_Store
node_modules/
.env
.env.local
*.log

# Ruflo / AgentDB MCP runtime artifacts (not part of the marketplace)
agentdb.rvf
agentdb.rvf.lock
ruvector.db

# apex-execute local runtime state (machine-specific, not part of the marketplace)
.dev-plan-state/

# Local skill symlinks created by promote-to-loop (absolute paths, machine-specific)
.claude/skills/
